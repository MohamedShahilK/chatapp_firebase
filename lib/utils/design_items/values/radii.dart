import 'package:flutter/rendering.dart';

class Radii {
  // k6pxRadius
  static const BorderRadiusGeometry circular20 =
      BorderRadius.all(Radius.circular(20));
  // k54pxRadius
  static const BorderRadiusGeometry circular54 =
      BorderRadius.all(Radius.circular(54));
}
